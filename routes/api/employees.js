const express = require("express");
const router = express.Router();
const employeeController = require("../../controllers/employeeController");

// router.post("/", employeeController.addUser);

module.exports = router;
